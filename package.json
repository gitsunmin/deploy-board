{
  "private": true,
  "name": "deploy-board",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "bunx biome lint --write .",
    "gen:graphql-type": "turbo run gen:graphql-type",
    "format": "bunx biome format --write ."
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.5.2"
  },
  "packageManager": "bun@1.2.11",
  "workspaces": ["apps/*", "packages/*"],
  "dependencies": {
    "ts-pattern": "^5.7.0"
  }
}
